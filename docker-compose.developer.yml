version: "3.6"
services:
  postgres:
    ports:
      - 5432:5432
    volumes:
      - /data/api/database:/var/lib/postgresql/data

networks:
  internal:
    external: false
    ipam:
      driver: default
      config:
        - subnet: 172.40.4.0/24